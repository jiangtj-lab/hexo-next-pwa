/* global importScripts, workbox */

'use strict';

importScripts('<%= workboxCDN %>');

<%= importDefine %>

<% if (Array.isArray(importScripts) && importScripts.length > 0) { %>
importScripts(
  <%- importScripts.map(JSON.stringify).join(',\n  ') %>
);
<% } %>

<% if (runtimeCaching) { %><%- runtimeCaching.join('\n') %><% } %>

<% if (offlineGoogleAnalytics) { %>workbox.googleAnalytics.initialize();<% } %>

<% if (disableDevLogs) { %>self.__WB_DISABLE_DEV_LOGS = true;<% } %>
